[project]
name = "market-lens-dashboard"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic-settings",
    "sqlalchemy",
    "market-lens-utils",
]

[tool.setuptools.packages.find]
where = ["."]

[tool.uv.sources]
market-lens-utils = { workspace = true }
